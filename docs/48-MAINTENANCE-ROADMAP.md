# 上线后维护清单

建立日期：2026-09-24
状态：M-01～M-03、M-05 完成；M-04 进行中（等高线语义与表现讨论）。

本文件是后续维护任务的唯一进度入口。`docs/08-REVIEW-CHECKLIST.md` 保留逐阶段执行记录，`docs/47-PUBLIC-RELEASE-CLOSEOUT.md` 保留上线验收结果；不要在三处分别维护任务状态。开始新一轮工作前，先读本文件并检查 Git 状态。任务只用四种状态：`待做`、`进行中`、`待 Henry 验收`、`完成`；原则上同时只有一项 `进行中`。每项完成时记录实际测试、截图位置、提交号与未验证项，再进入下一项；不能把计划写成已经验证的结果。

## 近期任务

| ID | 状态 | 工作范围 | 完成标准 |
| --- | --- | --- | --- |
| M-01 | 完成 | 修复世界地图标签在缩放和平移时的闪烁。检查标签上限的缩放阈值、画布边缘剔除、避让重排和选中主题的稳定性。 | 固定尺寸和倍率平移时，仍在画面内的同一标签不因其他标签出界而跳位或闪烁；跨缩放层级的出现/消失有可理解的规则；选中主题始终可辨；桌面、390px 和 320px 的连续手势与点击命中回归通过，截图/录屏对照留在本机。保持全部公开点坐标、标签归属和地图范围不变。 |
| M-02 | 完成 | 将 `README.md` 改成上线后的项目入口，准确说明网站、公开/本机数据范围、安装、构建、验证与部署方式；历史施工细节继续留在 `docs/`。 | README 不再声称尚未部署，不把本机数据命令误写成普通访客运行前提；所有命令与相对链接经过检查；不改 `/design` 已认可文案。 |
| M-03 | 完成 | 整理维护代码和公开仓库：逐项盘点根目录 Playwright 配置、E2E 截图/审计脚本、npm 脚本及公开文档，明确保留、归档或移除。 | 保留仍被调用的回归测试；变更引用后类型检查、相关 Playwright 和公开构建通过；不批量删历史文件或重写 Git 历史。对拟保留的仓库内容继续做人工公开性审阅，发现具体问题先记录受影响文件和处理方案，不直接删仓重建。 |
| M-04 | 进行中 | 以公开划线的二维局部密度为首要语义，对照同点位的密度采样精度、等高线、等值带和 2D 渲染技术；必要时可以在隔离实验中重新计算本机 embedding/UMAP，但不得混同画法对照或覆盖已审核点位。3D 暂缓。 | 用同数据、同视口检查聚集区的可见度和地图可用性，记录网格/模型改变带来的不同，测试手机、缩放、键盘和资源边界后由 Henry 选择是否替换。远程 embedding provider 仍需单独许可；正式公开范围和部署仍单独决定。证据见 `docs/49–53`。 |
| M-05 | 完成 | 保持现有 `/design` 精炼文案，另写一篇可选的技术延伸文章。 | 解释真实数据到公开投影、按书公平、标签/小径、embedding 与二维地图、静态交付及局限；与站内已实现行为核对；不公开凭证、审核细节、未批准内容或本机参考截图；正文与入口由 Henry 验收。 |

M-01 先于 M-04：先稳定地图标签，才比较等高线的视觉差异。M-02 与 M-03 可连续做，但各自记录验证结果，不因整理目录而中断现有命令。以上排序可随 Henry 的体验反馈调整。Henry 已授权本轮按清单推进相关维护任务；不包含自动推送或部署。

## 暂缓与独立验收

- **3D 地图：暂缓。** 先验收 2D 标签与等高线；若仍有明确的空间探索收益，再讨论独立的可切换 3D 视角、性能/触控成本及 2D 完整退路。flomo 截图是视觉参考，不作为仿制规格。本机参考素材位于 Git 忽略的 `.private/reference/visual-references/`，不得加入公开构建。
- **公开仓库审阅：未结案。** 2026-09-24 的初步检查覆盖当前跟踪路径、82 个提交的历史路径和常见凭证特征；未发现 `.private/`、`.env`、构建目录或凭证文件被跟踪，特征命中来自测试中的虚构 key。这不等于逐行人工内容审阅，也不等于划线和封面权利许可。若发现具体暴露，先评估已有克隆/缓存和修复范围，再决定是否需要迁移仓库。
- **发布后人工验收：仍开放。** 真实 Safari/iOS、读屏、浏览器菜单缩放、低亮度长时阅读、外部首次访客观察和引文/封面权利判断见 `docs/47-PUBLIC-RELEASE-CLOSEOUT.md`。Henry 已在笔记本和安卓 Chrome 访问并反馈体验良好；这不代替上述项目的验证。

## 执行记录

- 2026-09-24 · M-01 完成 · 标签避让改为固定地图基准布局，再按当前视图平移与裁切；总览固定桌面 18 / 窄屏 10 个优先标签，主题区域优先邻近标签。缩放时不再跨旧数量阈值整批切换，选中标签在画面内始终优先。点位、embedding、公开数据和等高线均未更改。`npm run check:local` 308/308；完整 local Chromium 130/130；`npm run build`、`npm run isolation:public`、`npm run publication:verify`、public Chromium 7/7、Pages 式 Chromium 5/5 通过；Pages 式 WebKit 与完整 E2E 并行时 4/5 超时或未稳定加载，单独以 90 秒超时复跑 5/5 通过。桌面/390/320px 的平移与指针锚点缩放截图在 `.private/review/maintenance/m01/`；新增几何单测和真实浏览器手势回归。真实手机浏览器触控仍待 Henry 验收；未推送或部署。

- 2026-09-24 · M-02 完成 · README 改成已上线的项目入口，分开说明公开可运行命令与依赖 `.private/` 的本机维护命令，补充部署自动触发边界。7 个相对链接逐项核对，旧的未部署文字已移除；M-01 阶段已实跑公开构建和浏览器检查，本阶段不改产品代码、不另称浏览器已重新执行。未推送。

- 2026-09-24 · M-03 完成 · 把 12 个历史截图/专项审计 Playwright 配置归档到 `e2e/config/`，根目录保留 5 个日常配置；更新 npm 命令、配置相对路径和相关注释，不删除历史测试及执行记录。全部 12 个配置 `--list` 均发现原测试；迁移后的 7I 审计 3/3、`npm run capture:v2e -- --list`、typecheck、lint、公开构建通过。仓库历史文件名检查（437 路径）未命中私有/凭证类路径，当前跟踪文件无常见密钥签名；这是有限技术检查，内容和权利判断仍开放。无重新导出、推送或部署。

- 2026-09-24 · M-04 待 Henry 验收 · 用 dev-only `d3-contour` 在相同公开点位/64 x 40 密度场/三个阈值上生成插值轮廓候选；Pages 式 Chromium 在内存中替换轮廓，产出世界/区域各 1440/390px 的当前版与候选版 8 张配对截图到 `.private/review/maintenance/m04/`。`npm run check:local` 309/309，build、public isolation、publication verify、候选 Chromium 1/1 通过；生产 JS hash 不变，未改公开快照或线上地图。初次候选测试因配置工作目录找不到预览脚本，设定仓库根 cwd 后通过；详见 `docs/49-FIXED-POINT-CONTOUR-COMPARISON.md`。真实审美选择待 Henry；未推送或部署。

- 2026-09-24 · M-05 待 Henry 验收 · 新增独立 `/design/technical/`，`/design` 原正文只增加一个可选入口；文章说明本机内容准备与公开投影、按书公平、两层主题、小径节奏、固定点位的二维地图及静态交付边界。代码提交 `1fd5c30`。`npm run check:local` 309/309，完整 local Chromium 130/130，`npm run build`、`npm run isolation:public`、`npm run publication:verify`（184 个静态房间）及 public Chromium 8/8 通过；Pages 式 Chromium 6/6、独立 WebKit 6/6。合并运行时 WebKit 曾因公开快照加载超时失败，单独运行还暴露旧封面断言未等待图片解码；等待加载后独立重跑通过。1440/390/320px 全页截图在 `.private/review/maintenance/m05/`，720px（200% 等效）仅测试未截图；`/design` 正文未改，文章内容与入口仍待 Henry 视觉与文案验收。真实 Safari/iOS、读屏与权利判断仍开放；未推送或部署。

- 2026-09-24 · M-05 完成；M-04 进行中 · Henry 认可技术文章与全站链接职责，要求“关于”把运作方式和技术实现并列提供，而非新增首页入口；本地提交 `2d4f4ba` 完成双入口与 320px 短链接不拆词。`npm run check:local` 309/309、`npm run build`、`npm run isolation:public`、`npm run publication:verify`、public Chromium 8/8、相关 local Chromium 1/1、Pages 式 Chromium 7/7 和 WebKit 7/7 通过；在重建后重新拍摄并检查 `.private/review/maintenance/m05/about-links-{1440,390,320}.png`。初次截图捕获了入场动画且样式修改后曾误用旧 `dist`，两处均已纠正，不能以旧图作验收。未推送或部署。等高线不再仅以平滑程度决定：`docs/50-READING-WORLD-CONTOUR-SEMANTICS.md` 区分 flomo 的笔记山峰叙事、本站划线二维密度的实际含义，以及尚未验证的跨书相遇层；地图代码、点位和线上视觉未改。下一步是与 Henry 确定等高线要帮访客看见什么，再设计同点位对照。

- 2026-09-25 · M-04 进行中 · Henry 明确选择以“划线在哪里聚集”作为等高线的第一含义。离线候选在同一 3,462 个固定点位和同一 64 x 40 公开密度场上新增 136/184/208/232 等层级；审计高峰附近的书籍构成，世界/区域在 1440/390/320px 各拍现版/候选，共 12 张 `.private/review/maintenance/m04/relief/` 截图。总览的局部高峰更清楚，区域视图新增线条偏密，未通过直接替换的视觉判断。`npm run check:local` 310/310、`npm run build`、`npm run isolation:public`、`npm run publication:verify` 和 Pages 式候选 Chromium 1/1 通过；首次截图断言错误假设窄屏/区域画布宽度，修正为确认画布有效后重跑通过。现有产品渲染、坐标、embedding、公开快照未改，未推送或部署。具体判断见 `docs/50`；下一步讨论是否仅在总览强调高峰、放大时让位于点与文字。

- 2026-09-25 · M-04 进行中 · Henry 允许除 3D 以外的广泛实验，包括必要时在隔离条件下重算 embedding；正式使用新点位和发布仍待选择。离线密度研究保持 3,462 个公开点位、原始平滑的地图空间尺度与相同标尺，64 x 40 完全复现发布密度，比较 128 x 80 / 256 x 160 的七层线稿；Pages 式 Chromium 在相同世界/区域与 1440/390/320px 采集 18 张网格截图，并在世界视图采集 6 张等值带/WebGL2 样板，均在 `.private/review/maintenance/m04/resolution/`。128 x 80 减少了网格感；256 x 160 增益较小；两种填色均仅是轻微变化，WebGL 首次透明合成过亮，经修正后重拍，不能据此宣称产品渲染性能获益。`npm run check:local` 312/312、build、public isolation、publication verify、terrain study Chromium 2/2 通过；详见 `docs/51-DENSITY-RESOLUTION-AND-RENDERING-STUDY.md`。没有动产品地图、公开快照或原本机布局；无推送部署。

- 2026-09-25 · M-04 进行中 · 复看本机 flomo 界面图与分享海报后，将“坐标底纹”与“密度单元格纹”分开控制：仅在 Pages 式 Chromium 测试浏览器中替换原 1,000 单位格线和逐格填色，保留相同 3,462 点/56 标签、128 x 80 密度和七层轮廓。世界/交易主题区域 × 1440/390/320px × 现版、小十字+淡等值带、小十字+连续 Canvas2D、小十字+WebGL2、极淡细格线+等值带、无坐标记号+等值带，共 36 张本机截图；放大后等值带曾显得像实体领地，降低透明度后重拍。界面推荐继续对照小十字+等值带和小十字+连续明暗；极淡格线暂限静态分享设计探索。`npm run check:local` 312/312、`npm run build`、`npm run test:terrain:study` 3/3、`npm run isolation:public`、`npm run publication:verify` 通过，常规 E2E 仍 130 项。测试检验底纹和逐格填色实际被替换、底色非空及无横向溢出；真实手机/Safari、交互持续重绘、GPU 回退、放大与分享图未验收。详见 `docs/52-MAP-SURFACE-REFERENCE-STUDY.md`。地图代码、快照、点位和公开构建未更改；无推送部署。

- 2026-09-25 · M-04 待 Henry 视觉与交互验收 · 在独立 `dev:map:study` 本机模式实现可操作地图候选：同一公开 3,462 点/56 标签，128 x 80 密度、七档线、小十字、WebGL2 连续明暗叠低强度淡等值带；WebGL2 不可用、着色器失败或上下文丢失则回退 Canvas 等值带。总览点先锚点放大，桌面约 3x/手机约 4x 后独立点直达原详情，8px 邻近重叠点先以原底部读数和细环预选、再点确认；无新面板，拖动/双指直接控制，reduced motion 不作长过渡。截图位于 `.private/review/maintenance/m04/interactive/`，源码和浏览器结果见 `docs/53-LOCAL-INTERACTIVE-MAP-CANDIDATE.md`。`npm run check:local` 316/316、build、public isolation、publication verify、候选 Chromium 7/7、既有本机地图/标签回归 15/15 通过。原回归首次误连 5173 已运行的公开模式而失败 9/15，使用独立 5188 本机模式重跑全部通过。正式构建 JS/CSS hash 略变但候选数据及视觉分支未启用，快照和线上地图未改；真机/Safari/读屏、长时间交互帧率、Henry 视觉 Gate 未验收，未推送部署。等高线悬停后置。

- 2026-09-25 · M-04 待 Henry 视觉与交互验收 · Henry 认可本机地形候选，但指出密集点的“预选后再点一次”会令首次访客困惑。候选模式现保持原 3x/窄屏 4x 放大门槛，达到可选尺度后 13px 内直接匹配屏幕最近的真实点并一次打开详情；不再画预选环或要求二次点选。鼠标/笔悬停点时用柔和暖光晕与亮芯区别于已打开划线的清晰描边；悬停等高线时只提亮相接的一圈，不使整层同时高亮，也不把线变为可点击主题。标签优先，触屏不保留悬停；移开、拖动、缩放都清理悬停态。真实世界图和区域图的候选截图与测试、初版对比见 `docs/53-LOCAL-INTERACTIVE-MAP-CANDIDATE.md` 及 `.private/review/maintenance/m04/interactive/hover-*` / `one-click-overlap-*`。候选 Chromium 9/9 覆盖单点、密集点一次到达、悬停区分与清理、线非点击、GPU 回退和触屏；纯函数补充同层轮廓连通性与最近点单测。`npm run check:local` 315/315、`npm run build`、`npm run isolation:public`、`npm run publication:verify`、既有本机地图/标签回归 15/15（独立端口 5188）、公开 Chromium 8/8 通过；完整单测初跑 314/315 因误把左上角测试点当作远离轮廓处，修正测试点后全绿。公开快照、正式地图、坐标与线上视觉未改；本阶段仍不推送部署，真机/Safari/读屏与 Henry 最终视觉验收待做。

- 2026-09-25 · M-04 待 Henry 新版地名视觉验收 · 在仅本机 `map-study` 候选里，将点详情的封面色外环统一为暖白色，封面色仍保留详情与 Book Aura；主题名去实心矩形，改为深色贴字暗衬、真实锚点和避让短引线，悬停增亮/短线，保留标签优先与不可见点击范围。`docs/53` 与 `.private/review/maintenance/m04/interactive/place-name-*`、`world-*` 记录 1440/390/320px 截图。`npm run check:local` 316/316、候选 Chromium 10/10、本机地图/标签 15/15、公开 Chromium 8/8、build/isolation/publication verify 通过。公开地图、点位、快照和线上网站未变；无推送部署。真机/Safari/读屏、浏览器 200% 和持续性能继续待验收。

## 更新格式

任务状态改变时，在对应 ID 下追加一条简短记录：`日期 · 状态 · 范围/决定 · 实际命令及结果 · 本机证据（若有） · 提交号 · 遗留问题`。同时按 `docs/08` 的约定记录完成阶段；不要为待做任务填写预期测试结果。公开仓库中的记录仅包含适合公开的信息。
