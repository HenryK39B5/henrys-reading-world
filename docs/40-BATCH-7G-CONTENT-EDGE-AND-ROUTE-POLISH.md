# 40 — Batch 7G: 全内容边界与跨页回程收口

> 日期：2026-09-23
>
> 状态：用户体验并认可7F整站深色方向，授权继续做产品级细节收口。本轮只修真实内容暴露的书房边界，并扩展浏览器审计；**不以自动化验收代替真机、读屏或最终视觉Gate**。

## 真实内容审计

`e2e/audit-7g.spec.ts` 只在本机真实 snapshot 存在时运行；显式命令：

```powershell
$env:AUDIT_PHASE='before'; npx playwright test --config playwright.audit-7g.config.ts --workers=1
$env:AUDIT_PHASE='after'; npx playwright test --config playwright.audit-7g.config.ts --workers=1
```

逐页访问130本书与56条小径，在320×720与1440×900下测标题和正文的文字边界、纵向相交、横向滚动；两版均为 **0硬性几何错误**，不捏造问题。`/books` 展开全部130本后检查每个书目标题；`/paths` 检查全部56条入口。额外在320、390、720 CSS px 检查真实无封面书 `b-040`、`b-081`、`b-130` 和最长标题 `b-094`、`b-099`，15个边界场景无横向溢出；后者720×450是200%等价布局视口，不声称真实浏览器缩放已经验证。报告和截图仅写 `.private/review/v3-private-atlas/7g/{before,after}/`，原文/封面不进入Git。

## 由截图发现并修复

- `b-099` 的60字真实书名在320px旧网格里持续被压在封面右侧，占去首屏大半，正文很晚出现；`b-094` 的41字标题也类似。仅对较长真实书名的手机书房，让封面浮在标题开头，文字经过封面后占完整阅读宽度；作者和书架在其后换行。`b-099` 新版正文从约y=588开始；所有书名完整，不截断、不缩小正文。对照 `before/narrow-b-099.png` 与 `after/320-cover-b-099.png`。普通标题保持原有网格。
- 三本无封面书的60×87缩略位原来把真实书名挤成细小而重复的文字；现在在书房和书目中显示诚实的「无封面」，完整真实书名仍在标题。对照 `before/narrow-cover-b-040.png` 与 `after/320-cover-b-040.png`。未制造假封面、未修改书籍元数据或封面来源。封面可加载的截图等到真实图片解码，避免把加载中的空框误判为最终视觉。
- 新增 `e2e/7g-navigation.spec.ts`：390px真实地图详情 `h-013` → 所属书房 → 返回同一原文 → 小径 → 书房 → 返回同一句 → 分享/ESC回焦 → 地图；未修改路由或公平算法。
- 完整套件首次127/128：降级 `showModal` 的旧分享滚轮测试偶发记录到弹层初始化产生的 `scrollY=0` 事件（复跑5次失败1次）。测试先确认弹层打开后的阅读位置稳定，再开始记录**用户滚轮期间**的文档滚动；原有零事件和回程位置要求未降低。修正后连续10/10，最终完整套件128/128。生产分享逻辑未改。

## 实测与剩余工作

`npm run check:local`：300/300，类型与lint通过；`npx playwright test --workers=1 --reporter=dot`：128/128；`playwright.audit-7g.config.ts` 最终2/2；`npm run build`、`npm run isolation:public`、`npm run test:public`：通过，公开快照仍为空。Studio本阶段未重跑，之前已通过；没有安装新依赖，没有更改真实地图坐标、标签或导出范围。

**未验证**：此机只装Chromium；Safari、真实手机、读屏、系统浏览器200%缩放和长时间深色阅读舒适度仍待实际环境。下一步适合让首次访客完整走一轮世界→文字→书→小径的路径，再根据实际阻塞决定是否恢复7B/7C；在此之前不把「商业级」视为自动通过。正式public export、covers、push、部署继续单独Gate。
